You are a frontend developer. Implement all application code based on the plan below.

## Plan

{{PLAN_JSON}}

## Project Directory

{{PROJECT_DIR}}

## Instructions

1. Read the existing scaffolded project structure first.

2. Implement every page, component, and feature listed in the plan.

3. Code quality requirements:
   - TypeScript with proper types (no `any`)
   - All components must be functional and render correctly
   - Responsive design (mobile-first)
   - Semantic HTML
   - Accessible (aria labels, alt text, proper heading hierarchy)
   - No placeholder text like "Lorem ipsum" — use realistic content matching the project theme

4. Styling:
   - Follow the styling approach from the plan
   - Consistent color scheme and typography
   - Proper spacing and layout
   - Mobile breakpoints

5. Functionality:
   - All links and navigation must work
   - Forms must have validation (client-side)
   - Interactive elements must have hover/focus states
   - If the plan includes a cart/list, implement with local state

6. After writing all code, run `npm run build` to verify it compiles.
   If the build fails, fix the errors immediately.

7. Do NOT modify package.json unless absolutely necessary for missing dependencies.

Write production-quality code. This will be deployed directly.
